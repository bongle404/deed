---
phase: 01-rea-domain-portal-integration
plan: 05
type: execute
wave: 3
depends_on:
  - "01-03"
  - "01-04"
files_modified: []
autonomous: false
requirements:
  - PORTAL-01
  - PORTAL-02
  - PORTAL-03
must_haves:
  truths:
    - "Seller sees the portal opt-in card in sell.html Step 6 and can toggle it on/off"
    - "Go Live button is blocked when portal opt-in is checked but fee acknowledgement is unchecked"
    - "Dashboard portal status card shows correct badge for each rea_status value (pending, live, rejected)"
    - "All automated Jest tests pass before checkpoint begins"
  artifacts:
    - path: "api/submit-to-portals.js"
      provides: "Verified working handler"
    - path: "api/portal-status.js"
      provides: "Verified working handler"
    - path: "sell.html"
      provides: "Verified opt-in UI in Step 6"
    - path: "dashboard.html"
      provides: "Verified portal status card"
  key_links: []

user_setup: []
---

<objective>
Human verification checkpoint: confirm the complete Phase 1 portal integration works end-to-end in the browser before marking the phase done. All automated tests must be green before this checkpoint begins.

Purpose: PORTAL-02 and PORTAL-03 involve UI flows (sell step 6, dashboard badge rendering) that cannot be fully verified by automated tests alone. This checkpoint catches visual regressions and logic gaps that only appear in-browser.
Output: Ed confirms the sell flow opt-in and dashboard status panel work correctly. Phase 1 is marked complete.
</objective>

<execution_context>
@/Users/edsteward/.claude/get-shit-done/workflows/execute-plan.md
@/Users/edsteward/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-rea-domain-portal-integration/01-03-SUMMARY.md
@.planning/phases/01-rea-domain-portal-integration/01-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run full Jest suite and confirm all tests green</name>
  <what-built>
    Gate check before human verification — all Jest tests must pass before proceeding.

    Run this command and confirm zero failures:
    ```bash
    cd /Users/edsteward/deed && npx jest --no-coverage 2>&1 | grep -E "Tests:|Test Suites:|passed|failed"
    ```

    Expected passing suites:
    - api/__tests__/submit-to-portals.test.js — 5 tests
    - api/__tests__/portal-status.test.js — 4 tests
    - api/__tests__/broker-register.test.js
    - api/__tests__/broker-submit-buyer.test.js
    - api/__tests__/buyer-activate.test.js
    - api/__tests__/broker-resend-activation.test.js
    - api/__tests__/developer-api.test.js

    If any test fails, diagnose and fix before proceeding to the in-browser check.
  </what-built>
  <how-to-verify>
    Run the command above. Confirm the output shows "X passed" with 0 failed.
  </how-to-verify>
  <resume-signal>Type "tests green" when all tests pass, or describe any failures so they can be fixed first.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: In-browser verification of sell flow and dashboard</name>
  <what-built>
    Phase 1 REA/Domain Portal Integration — four components:
    1. sell.html Step 6 — portal opt-in toggle + fee disclosure card (PORTAL-02)
    2. api/submit-to-portals.js — REAXML generation + SFTP delivery with credential guard (PORTAL-01)
    3. api/portal-status.js — GET handler for dashboard polling (PORTAL-03)
    4. dashboard.html — portal distribution status card with badge labels (PORTAL-03)

    Commercial prerequisites NOT yet met (these are required before going live):
    - QLD corporate agent licence for DEED Pty Ltd (confirm with OFT — individual licence alone may not suffice)
    - REA Ignite agency account with SFTP credentials (contact reaagents@rea-group.com — 2-4 week lead time)
    - Portal listing fee price decision — [PORTAL_FEE_PLACEHOLDER] still in sell.html (Ed must set this)
    - Supabase migration SQL must be run manually in Supabase SQL Editor (see supabase-schema.sql)

    The SFTP submission is guarded — without REA credentials in Vercel env vars, the handler returns dev_skipped safely and sets rea_status='not_submitted'. No real listings will be submitted until credentials are set.

    Note: domain-badge in the dashboard card will show "Not opted in" until Domain portal integration is built. Domain submission is a Phase 1b item — domain_status will always be not_submitted in this phase.
  </what-built>
  <how-to-verify>
    Run the project locally:
    ```bash
    cd /Users/edsteward/deed && vercel dev
    ```
    (Or open sell.html and dashboard.html directly if Vercel dev is not needed for this check.)

    **Check 1 — sell.html Step 6 portal opt-in card:**
    1. Open sell.html → progress through Steps 1-5 (or jump to Step 6 via browser devtools: document.getElementById('step-6').classList.add('active'))
    2. Confirm the "List on realestate.com.au & Domain" blue card appears ABOVE the "Confirm & Go Live" button
    3. Confirm [PORTAL_FEE_PLACEHOLDER] text is visible in the card body (the fee amount has NOT been invented)
    4. Click the "Yes — list my property on realestate.com.au and Domain" checkbox
    5. Confirm a second checkbox "I understand the additional portal listing fee is payable on settlement." appears beneath it
    6. Click "Confirm & Go Live" WITHOUT checking the second checkbox — confirm it is blocked (alert or validation fires)
    7. Check both checkboxes — confirm Go Live proceeds normally

    **Check 2 — dashboard.html portal status card:**
    1. Open dashboard.html?listing_id=any-valid-id
    2. Confirm no portal status card is visible when portal_opted_in=false (card hidden by default — correct)
    3. Simulate a portal_opted_in=true listing: In Supabase, set portal_opted_in=true and rea_status='pending' for a test listing, then reload the dashboard. Confirm the portal status card appears with "Processing…" in amber next to realestate.com.au
    4. Set rea_status='live' and rea_listing_url='https://www.realestate.com.au/property-house-qld-burleigh+heads-12345' in Supabase. Reload. Confirm badge shows "Live" in green AND the "View on realestate.com.au →" link appears.
    5. Set rea_status='rejected'. Reload. Confirm badge shows "Rejected — contact DEED" in red.
    6. Confirm domain-badge shows "Not opted in" — this is expected (Domain integration is Phase 1b).

    **Check 3 — Supabase migration reminder:**
    Confirm you have run (or will run before testing with real Supabase data) the migration SQL from supabase-schema.sql in the Supabase SQL Editor. Without this, the portal columns do not exist and the API handlers will error.
  </how-to-verify>
  <resume-signal>Type "approved" when all checks pass, or describe any issues found so they can be fixed before phase completion.</resume-signal>
</task>

</tasks>

<verification>
Phase 1 is complete when:
1. All Jest tests pass (9 new tests + 5 existing = 14 total)
2. Ed has typed "approved" after verifying the sell flow opt-in and dashboard status card in-browser
3. Commercial prerequisites (licence, REA account, fee price, DB migration) are documented and understood as outstanding — they do not block code completion but must be resolved before real listings can be submitted to REA
</verification>

<success_criteria>
- All automated Jest tests green (zero failures)
- sell.html Step 6 portal opt-in card verified in browser
- dashboard.html portal status card shows correct badges for pending, live, rejected states
- domain-badge shows "Not opted in" — expected, Domain is Phase 1b
- Ed has confirmed "approved" on this checkpoint
- Phase 1 PORTAL-01, PORTAL-02, PORTAL-03 requirements are marked complete in ROADMAP.md and REQUIREMENTS.md
</success_criteria>

<output>
After completion, create `.planning/phases/01-rea-domain-portal-integration/01-05-SUMMARY.md`

Then update:
- .planning/ROADMAP.md — Phase 1 plans list updated, mark phase as complete when all 5 plans done
- .planning/REQUIREMENTS.md — PORTAL-01, PORTAL-02, PORTAL-03 marked [x]
- .planning/STATE.md — Phase 1 marked complete, Phase 2 ready to plan
</output>
