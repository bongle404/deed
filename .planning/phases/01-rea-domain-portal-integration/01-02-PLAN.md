---
phase: 01-rea-domain-portal-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase-schema.sql
  - sell.html
autonomous: true
requirements:
  - PORTAL-02
must_haves:
  truths:
    - "Seller sees a portal opt-in toggle and fee disclosure card in Step 6 before the Go Live button"
    - "The fee disclosure card states the portal fee amount clearly (placeholder pending Ed's price decision)"
    - "Seller cannot proceed to Go Live if they check portal opt-in but do not acknowledge the portal fee"
    - "goLive() saves portal_opted_in and portal_fee_acknowledged to Supabase when seller opts in"
    - "goLive() saves portal_opted_in=false when seller does not opt in (no portal fee saved)"
    - "portal_opted_in=true triggers a POST to /api/submit-to-portals after the listing is saved"
  artifacts:
    - path: "supabase-schema.sql"
      provides: "Schema documentation for the 7 new portal columns (migration SQL to run in Supabase)"
      contains: "portal_opted_in, portal_fee_acknowledged, rea_status, domain_status, rea_listing_url, domain_listing_url, portal_submitted_at, portal_error"
    - path: "sell.html"
      provides: "Portal opt-in toggle + fee disclosure card in Step 6, goLive() updated"
      contains: "portal-opt-in, portal-fee-ack, goLive portal submission call"
  key_links:
    - from: "sell.html goLive()"
      to: "Supabase listings table"
      via: "db.from('listings').insert([{ portal_opted_in, portal_fee_acknowledged, ... }])"
      pattern: "portal_opted_in"
    - from: "sell.html goLive()"
      to: "/api/submit-to-portals"
      via: "fetch('/api/submit-to-portals', { method: 'POST', body: { listing_id } })"
      pattern: "submit-to-portals"

user_setup: []
---

<objective>
Add the DB migration SQL and the sell.html Step 6 portal opt-in UI so sellers can choose to list on REA/Domain with explicit fee disclosure before going live. goLive() is updated to save portal fields and trigger portal submission.

Purpose: Delivers PORTAL-02 — the seller-facing opt-in moment. This is the commercial gate: sellers who opt in are authorising portal distribution and acknowledging the fee.
Output: supabase-schema.sql updated with migration SQL block; sell.html Step 6 has portal toggle + fee card; goLive() saves portal fields and calls /api/submit-to-portals when opted in.
</objective>

<execution_context>
@/Users/edsteward/.claude/get-shit-done/workflows/execute-plan.md
@/Users/edsteward/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-rea-domain-portal-integration/01-RESEARCH.md

<interfaces>
<!-- sell.html Step 6 structure (current, from codebase read): -->
<!-- Line 891: <div class="step-panel" id="step-6"> -->
<!-- Line 925-928: existing fee-acknowledgement checkbox -->
<!-- Line 930-933: <button id="go-live-btn" onclick="goLive()"> ... Confirm & Go Live </button> -->
<!-- Portal opt-in card must be inserted AFTER the existing fee-acknowledgement checkbox (line 928) and BEFORE go-live-btn (line 930). -->

<!-- goLive() structure (line 1141): -->
<!-- 1. Validates fee-acknowledgement checkbox -->
<!-- 2. Disables button, shows "Going live..." -->
<!-- 3. Reads form fields (address, suburb, postcode, price, etc.) -->
<!-- 4. Inserts to db.from('listings') -->
<!-- 5. Uploads photos -->
<!-- 6. Fires notify calls -->
<!-- New portal logic goes: after step 4 resolves (has listingId), if portal_opted_in=true, fire fetch('/api/submit-to-portals') -->

<!-- Supabase client: const db = window.supabase (deed-supabase.js). Same client used throughout sell.html. -->

<!-- Migration SQL from research (verbatim, safe to append to supabase-schema.sql): -->
<!--
alter table listings add column if not exists portal_opted_in boolean default false;
alter table listings add column if not exists portal_fee_acknowledged boolean default false;
alter table listings add column if not exists rea_status text default 'not_submitted';
alter table listings add column if not exists domain_status text default 'not_submitted';
alter table listings add column if not exists rea_listing_url text;
alter table listings add column if not exists domain_listing_url text;
alter table listings add column if not exists portal_submitted_at timestamptz;
alter table listings add column if not exists portal_error text;
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append portal migration SQL to supabase-schema.sql</name>
  <files>supabase-schema.sql</files>
  <action>
    Read supabase-schema.sql and append a clearly labelled migration block at the end of the file:

    ```sql
    -- ── Phase 1: REA/Domain Portal Integration ──────────────────────────────
    -- Run this block in Supabase SQL Editor (Dashboard → SQL Editor → New query)
    -- Safe to re-run: all statements use "add column if not exists"

    alter table listings add column if not exists portal_opted_in boolean default false;
    alter table listings add column if not exists portal_fee_acknowledged boolean default false;
    alter table listings add column if not exists rea_status text default 'not_submitted';
      -- values: not_submitted | pending | live | rejected | withdrawn
    alter table listings add column if not exists domain_status text default 'not_submitted';
      -- values: not_submitted | pending | live | rejected | withdrawn
    alter table listings add column if not exists rea_listing_url text;
    alter table listings add column if not exists domain_listing_url text;
    alter table listings add column if not exists portal_submitted_at timestamptz;
    alter table listings add column if not exists portal_error text;
    -- ────────────────────────────────────────────────────────────────────────
    ```

    This file is documentation only — the actual migration must be run by the DEED operations team in the Supabase SQL Editor. Do NOT run the SQL via a script at this stage.

    IMPORTANT: The migration SQL must be run in Supabase before the sell flow opt-in can save portal fields. The executor should add a comment at the top of the migration block flagging this.
  </action>
  <verify>
    <automated>grep -c "portal_opted_in" /Users/edsteward/deed/supabase-schema.sql</automated>
    Expected output: 1 (line present in file)
  </verify>
  <done>supabase-schema.sql contains the 8-column portal migration block with if not exists guards and status value comments</done>
</task>

<task type="auto">
  <name>Task 2: Add portal opt-in toggle + fee card to sell.html Step 6 and update goLive()</name>
  <files>sell.html</files>
  <action>
    Read sell.html. Make two targeted edits:

    **Edit 1 — Insert portal opt-in UI after the existing fee-acknowledgement label (after line 928) and before the go-live-btn button:**

    Insert this HTML block:

    ```html
    <!-- Portal distribution opt-in — Phase 1 -->
    <div style="background:#f0f9ff;border:1px solid #bae6fd;border-radius:10px;padding:1.25rem;margin-bottom:1.5rem;">
      <div style="font-size:0.825rem;font-weight:600;color:#0369a1;margin-bottom:0.5rem;">List on realestate.com.au &amp; Domain</div>
      <p style="font-size:0.82rem;color:#374151;line-height:1.5;margin:0 0 1rem;">
        Reach buyers browsing REA and Domain in addition to DEED's verified network.
        DEED handles submission on your behalf as a QLD-licensed agency.
        An additional portal listing fee of <strong>[PORTAL_FEE_PLACEHOLDER]</strong> is payable on settlement.
      </p>
      <label style="display:flex;align-items:flex-start;gap:0.75rem;cursor:pointer;margin-bottom:0.75rem;">
        <input type="checkbox" id="portal-opt-in" style="margin-top:3px;width:18px;height:18px;flex-shrink:0;accent-color:#0369a1;" onchange="togglePortalFeeAck(this.checked)" />
        <span style="font-size:0.875rem;line-height:1.5;">Yes — list my property on realestate.com.au and Domain</span>
      </label>
      <label id="portal-fee-ack-row" style="display:none;align-items:flex-start;gap:0.75rem;cursor:pointer;">
        <input type="checkbox" id="portal-fee-ack" style="margin-top:3px;width:18px;height:18px;flex-shrink:0;accent-color:#0369a1;" />
        <span style="font-size:0.875rem;line-height:1.5;">I understand the additional portal listing fee is payable on settlement.</span>
      </label>
    </div>
    ```

    **Edit 2 — Add togglePortalFeeAck() helper and update goLive() in the `<script>` block:**

    Add this helper function near the top of the script block (before goLive()):

    ```javascript
    function togglePortalFeeAck(checked) {
      const ackRow = document.getElementById('portal-fee-ack-row');
      ackRow.style.display = checked ? 'flex' : 'none';
      if (!checked) document.getElementById('portal-fee-ack').checked = false;
    }
    ```

    Update goLive() to:
    1. After validating fee-acknowledgement, also validate: if portal-opt-in is checked, portal-fee-ack must also be checked. If not: alert('Please acknowledge the portal listing fee to continue.') and return.
    2. Read portal_opted_in = document.getElementById('portal-opt-in').checked
    3. Read portal_fee_acknowledged = document.getElementById('portal-fee-ack').checked
    4. Add portal_opted_in and portal_fee_acknowledged to the Supabase insert payload (alongside the existing fields)
    5. After the photo upload block (after the listingId is confirmed), add:

    ```javascript
    // Portal submission — fire-and-forget (status tracked via rea_status in Supabase)
    if (portalOptedIn) {
      fetch('/api/submit-to-portals', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ listing_id: listingId }),
      }).catch(() => {}); // non-fatal — status tracked in Supabase
    }
    ```

    Note on [PORTAL_FEE_PLACEHOLDER]: Ed has not yet set the portal fee price. Leave the literal string "[PORTAL_FEE_PLACEHOLDER]" in the HTML — the executor should NOT invent a price. This will be replaced once Ed confirms the amount.
  </action>
  <verify>
    <automated>grep -c "portal-opt-in\|portal-fee-ack\|submit-to-portals\|portal_opted_in" /Users/edsteward/deed/sell.html</automated>
    Expected: 4 or more (one match per term)
  </verify>
  <done>
    - sell.html Step 6 contains the portal opt-in card (visible, before go-live-btn)
    - togglePortalFeeAck() function exists in script block
    - goLive() validates portal fee acknowledgement if opted in
    - goLive() includes portal_opted_in and portal_fee_acknowledged in Supabase insert
    - goLive() calls /api/submit-to-portals when portal_opted_in=true
    - [PORTAL_FEE_PLACEHOLDER] is present in the fee disclosure text (not replaced with a made-up number)
  </done>
</task>

</tasks>

<verification>
1. Open sell.html in a browser (or inspect HTML). Navigate to Step 6. Confirm portal opt-in card appears above the Go Live button.
2. Check portal-opt-in toggle: clicking it should reveal the portal-fee-ack checkbox.
3. Inspect the Supabase insert call in goLive() (browser devtools or code review): confirm portal_opted_in and portal_fee_acknowledged fields are present.
4. Confirm [PORTAL_FEE_PLACEHOLDER] is still a placeholder — not a number Ed hasn't approved.
</verification>

<success_criteria>
- supabase-schema.sql has the 8-column portal migration block with if not exists guards
- sell.html Step 6 has the portal opt-in card inserted before go-live-btn
- goLive() validates portal acknowledgement, saves portal fields, triggers /api/submit-to-portals when opted in
- No portal fee dollar amount has been invented — placeholder only
</success_criteria>

<output>
After completion, create `.planning/phases/01-rea-domain-portal-integration/01-02-SUMMARY.md`
</output>
