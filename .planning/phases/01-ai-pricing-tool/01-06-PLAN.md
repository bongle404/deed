---
phase: 01-ai-pricing-tool
plan: 06
type: execute
wave: 4
depends_on:
  - 01-04
  - 01-05
files_modified: []
autonomous: false
requirements:
  - PRICE-01
  - PRICE-02
  - PRICE-03
  - PRICE-04

must_haves:
  truths:
    - "Seller sees low/mid/high estimate with comparable sales in sell.html before publishing"
    - "Estimate updates without page reload when beds/baths/size changes"
    - "Dashboard offer card shows 'Below market' flag with reason text when offer is below floor"
    - "Existing offer flow and dashboard still work without JS errors"
  artifacts:
    - path: sell.html
      provides: "Working price estimate widget with comps table"
    - path: dashboard.html
      provides: "Below-floor offer flag rendered on affected offer cards"
    - path: api/price-estimate.js
      provides: "Live endpoint returning real Proptech Data comparable sales"
  key_links:
    - from: sell.html
      to: /api/price-estimate
      via: "fetch POST call triggered by form inputs"
      pattern: "fetch.*price-estimate"
---

<objective>
Human verification of all four Phase 1 success criteria. Claude has automated everything — this checkpoint confirms it works as a seller would experience it.

Purpose: No automated test can verify visual rendering, form interaction, or real API responses. This checkpoint is the final gate before Phase 1 is marked complete.
Output: Confirmed working or issues list returned for gap closure.
</objective>

<execution_context>
@/Users/edsteward/.claude/get-shit-done/workflows/execute-plan.md
@/Users/edsteward/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/edsteward/deed/.planning/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Setup: Run DB migration and set API key</name>
  <what-automated>
    All code is complete. Two manual steps are needed before the UI can be tested with real data.
  </what-automated>
  <instructions>
    **Step 1: Run the DB migration**
    1. Open https://supabase.com/dashboard → your DEED project → SQL Editor
    2. Paste the contents of `supabase-migration-phase1.sql` (project root)
    3. Click Run
    4. Confirm no errors — the three sections add: comparable_sales_cache table, listings estimate columns, offers below_floor columns

    **Step 2: Add the Proptech Data API key**
    1. Sign up at proptechdata.com.au if you haven't (free trial — 100 req/day, 30 days)
    2. Copy your API key from their API Settings page
    3. Run: `vercel env add PROPTECH_DATA_API_KEY` in the deed directory and paste the key
    4. Redeploy: `vercel --prod --yes`

    **Step 3: Confirm Proptech Data QLD coverage** (important — do this before testing)
    - Make a quick test call to confirm beds/baths are in the response for a Gold Coast suburb
    - If beds/baths are missing from their API response, the filtering won't work — raise this before proceeding
  </instructions>
  <resume-signal>Type "setup done" when both migration and API key are in place, or describe any issues</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify: All four Phase 1 success criteria</name>
  <what-built>
    - /api/price-estimate endpoint with Proptech Data + Supabase cache
    - Price estimate widget in sell.html (low/mid/high + comps table + debounced recalc)
    - Below-floor offer detection in offer submission flow
    - Below-floor flag UI on dashboard.html offer cards
  </what-built>
  <how-to-verify>
    Open your deployed DEED app (https://deed-sooty.vercel.app) and run through each check:

    **Check 1 — PRICE-01: Estimate appears before publish**
    1. Go to sell.html, start a new listing
    2. Enter your property details including suburb and bedrooms
    3. Confirm a price estimate panel appears showing Low / Likely / High estimates with AU dollar amounts
    4. Confirm a confidence level label is shown (HIGH / MEDIUM / LOW)

    **Check 2 — PRICE-02: Comparable sales are shown**
    1. In the same listing flow, look below the estimate bands
    2. Confirm a table of comparable sales appears (address, sold price, bed/bath, sold date)
    3. Confirm the data is real (addresses and prices look plausible for the suburb you entered)

    **Check 3 — PRICE-04: Estimate recalculates on detail change**
    1. Still in the listing flow, change the bedrooms dropdown to a different value
    2. Wait 1 second (debounce is 600ms)
    3. Confirm the estimate panel shows a loading state briefly, then updates with new figures
    4. Confirm the comparable sales table also updates

    **Check 4 — PRICE-03: Below-floor offer flag on dashboard**
    1. Go to dashboard.html for an active listing
    2. To test the flag: either submit a test offer below the p25 estimate, or temporarily seed a below_floor=true record directly in Supabase for an existing offer
    3. Confirm the offer card shows an orange/red "Below market" label
    4. Confirm the label includes a sentence explaining why (with price amounts and percentage gap)

    **Regression check**
    - Confirm existing offer cards without below_floor=true show no flag
    - Confirm the existing listing flow still works end-to-end (no JS errors in browser console)
  </how-to-verify>
  <resume-signal>Type "approved" if all 4 checks pass, or describe which check failed and what you saw</resume-signal>
</task>

</tasks>

<verification>
Manual verification via browser — no automated command for this plan.

After approval, run final test suite to confirm nothing regressed:
```bash
cd /Users/edsteward/deed && npm test
```
</verification>

<success_criteria>
- All 4 Phase 1 success criteria confirmed by Ed in browser
- Test suite still fully GREEN after deployment
- Phase 1 can be marked complete in ROADMAP.md
</success_criteria>

<output>
After completion, create `.planning/phases/01-ai-pricing-tool/01-06-SUMMARY.md`

Also update:
- `.planning/ROADMAP.md` — mark Phase 1 complete
- `.planning/STATE.md` — update current position to Phase 2
- `.planning/REQUIREMENTS.md` — mark PRICE-01, PRICE-02, PRICE-03, PRICE-04 as complete
</output>
