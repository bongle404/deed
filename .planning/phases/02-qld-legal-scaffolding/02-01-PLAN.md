---
phase: 02-qld-legal-scaffolding
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - api/__tests__/disclosure.test.js
  - api/__tests__/disclosure-checklist.test.js
  - api/__tests__/generate-disclosure.test.js
autonomous: true
requirements:
  - LEGAL-01
  - LEGAL-02
  - LEGAL-04

must_haves:
  truths:
    - "Running npm test produces test output (even all-failing) within 15 seconds"
    - "disclosure.test.js covers publish gate (blocked without completed_at), form field validation, and Supabase upsert"
    - "disclosure-checklist.test.js covers all 8 checklist items deriving pass/fail/warning from disclosure field values"
    - "generate-disclosure.test.js covers 200+PDF buffer for valid listing_id, 404 for missing disclosure, 400 for missing listing_id, 405 for GET"
  artifacts:
    - path: api/__tests__/disclosure.test.js
      provides: Test stubs for LEGAL-01 (Form 2 gate and data persistence)
    - path: api/__tests__/disclosure-checklist.test.js
      provides: Test stubs for LEGAL-02 (checklist pass/fail derivation logic)
    - path: api/__tests__/generate-disclosure.test.js
      provides: Test stubs for LEGAL-04 (PDF generation API route)
  key_links:
    - from: api/__tests__/disclosure.test.js
      to: api/disclosure.js
      via: "require('../disclosure')"
      pattern: "require\\('../disclosure'\\)"
    - from: api/__tests__/disclosure-checklist.test.js
      to: api/helpers/disclosure-checklist.js
      via: "require('../../helpers/disclosure-checklist')"
      pattern: "require\\('../../helpers/disclosure-checklist'\\)"
    - from: api/__tests__/generate-disclosure.test.js
      to: api/generate-disclosure.js
      via: "require('../generate-disclosure')"
      pattern: "require\\('../generate-disclosure'\\)"
---

<objective>
Create failing test stubs for all three Phase 2 automated test files before any implementation exists. Tests must be RED (failing with "Cannot find module") when this plan completes — that is the correct Wave 0 state. Implementation in Wave 1 and Wave 2 plans will turn them GREEN.

Purpose: Nyquist compliance — every implementation task in subsequent plans has a pre-existing automated verify command with a defined expectation. Tests define the contract before code exists.
Output: Three test files covering LEGAL-01, LEGAL-02, and LEGAL-04. LEGAL-03 (conveyancer referral prompt) is UI-only and manual-verified — no test stub needed.
</objective>

<execution_context>
@/Users/edsteward/.claude/get-shit-done/workflows/execute-plan.md
@/Users/edsteward/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/edsteward/deed/.planning/PROJECT.md
@/Users/edsteward/deed/.planning/ROADMAP.md
@/Users/edsteward/deed/.planning/phases/02-qld-legal-scaffolding/02-RESEARCH.md
@/Users/edsteward/deed/.planning/phases/02-qld-legal-scaffolding/02-VALIDATION.md

Existing test pattern to follow (copy mock/require structure from these):
@/Users/edsteward/deed/api/__tests__/broker-submit-buyer.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write disclosure.test.js (LEGAL-01 stubs)</name>
  <files>api/__tests__/disclosure.test.js</files>
  <action>
Create test stubs for the disclosure form API handler (`api/disclosure.js` — does not exist yet). Follow the exact mock/require pattern from `broker-submit-buyer.test.js`.

Tests to write (all should fail with "Cannot find module ../disclosure"):

1. `returns 405 for GET requests` — makeReq with method GET → expect 405
2. `returns 400 if listing_id missing` — POST with empty body → expect 400 and error message
3. `saves disclosure data and sets completed_at` — mock Supabase upsert, POST with full valid disclosure body including `listing_id` and at least 5 form fields → expect Supabase upsert called with `completed_at` not null, response 200 with `{ok: true}`
4. `canPublish returns false when no disclosure record exists` — mock Supabase select returning null → expect response includes `published: false` or similar gate behaviour
5. `canPublish returns true when completed_at is set` — mock Supabase select returning row with `completed_at` → expect gate passes

Mock structure needed:
- Mock `@supabase/supabase-js` createClient with `from()` returning `upsert` and `select/eq/single` chains
- The handler will be at `api/disclosure.js` — `require('../disclosure')`

At the top of the file add: `// LEGAL-01: Form 2 disclosure form — save and publish gate`
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=disclosure 2>&1 | grep -E "Cannot find module|FAIL|PASS|Tests:"</automated>
  </verify>
  <done>File exists, 5 tests written, all fail with "Cannot find module ../disclosure" — RED state confirmed</done>
</task>

<task type="auto">
  <name>Task 2: Write disclosure-checklist.test.js (LEGAL-02 stubs)</name>
  <files>api/__tests__/disclosure-checklist.test.js</files>
  <action>
Create test stubs for the checklist derivation helper (`api/helpers/disclosure-checklist.js` — does not exist yet). This is a pure function — no Supabase mocking needed.

The helper exports `deriveChecklist(disclosure)` which takes a disclosure row object and returns an array of `{ label, status, note }` objects where `status` is `'pass' | 'fail' | 'warning' | 'info'`.

Tests to write (all should fail with "Cannot find module ../../helpers/disclosure-checklist"):

1. `pool: pass when pool_status is compliant` — `{has_pool: true, pool_status: 'compliant'}` → pool item has status 'pass'
2. `pool: warning when pool_status is non_compliant_form36` — `{has_pool: true, pool_status: 'non_compliant_form36'}` → pool item has status 'warning' and note mentions Form 36
3. `pool: pass when no pool` — `{has_pool: false, pool_status: 'no_pool'}` → pool item has status 'pass'
4. `body corp: pass when not community title` — `{has_community_title: false}` → body_corp item has status 'pass'
5. `body corp: warning when pending` — `{has_community_title: true, body_corp_cert_status: 'pending'}` → status 'warning', note mentions 5 business days
6. `ATO clearance: warning when not obtained` — `{ato_clearance_obtained: false}` → ato item has status 'warning', note mentions 15% withholding
7. `ATO clearance: pass when obtained` — `{ato_clearance_obtained: true}` → ato item has status 'pass'
8. `building notices: fail when present` — `{has_building_notices: true}` → building item has status 'fail'
9. `returns array of 8 items for complete disclosure` — full valid disclosure object → array length === 8

Use `require('../../helpers/disclosure-checklist')`.

At top: `// LEGAL-02: Prescribed certificate checklist — pass/fail derivation`
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=disclosure-checklist 2>&1 | grep -E "Cannot find module|FAIL|PASS|Tests:"</automated>
  </verify>
  <done>File exists, 9 tests written, all fail with "Cannot find module ../../helpers/disclosure-checklist" — RED state confirmed</done>
</task>

<task type="auto">
  <name>Task 3: Write generate-disclosure.test.js (LEGAL-04 stubs)</name>
  <files>api/__tests__/generate-disclosure.test.js</files>
  <action>
Create test stubs for the PDF generation handler (`api/generate-disclosure.js` — does not exist yet). Follow mock pattern from `broker-submit-buyer.test.js`.

The handler returns `application/pdf` on success. In tests, mock pdfmake to call the buffer callback with a Buffer containing the string "PDF".

Mock structure needed:
- Mock `@supabase/supabase-js` createClient
- Mock `pdfmake/build/pdfmake` — export object with `vfs` setter and `createPdf(docDef)` returning `{ getBuffer: fn => fn(Buffer.from('PDF')) }`
- Mock `pdfmake/build/vfs_fonts` — export `{ pdfMake: { vfs: {} } }`

Tests to write (all should fail with "Cannot find module ../generate-disclosure"):

1. `returns 405 for GET` — method GET → expect 405
2. `returns 400 if listing_id missing` — POST with empty body → expect 400
3. `returns 404 if no disclosure record found` — mock Supabase select returning null data → expect 404
4. `returns PDF buffer for valid listing_id` — mock Supabase returning full disclosure row with nested listings data → expect res.setHeader called with 'Content-Type' 'application/pdf', res.send called with a Buffer
5. `PDF response has correct Content-Disposition header` — same mock → expect setHeader called with 'Content-Disposition' containing 'disclosure-statement.pdf'

Use `require('../generate-disclosure')`.

At top: `// LEGAL-04: Generate disclosure statement PDF`
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=generate-disclosure 2>&1 | grep -E "Cannot find module|FAIL|PASS|Tests:"</automated>
  </verify>
  <done>File exists, 5 tests written, all fail with "Cannot find module ../generate-disclosure" — RED state confirmed</done>
</task>

</tasks>

<verification>
All three test files exist and all tests are RED (failing with "Cannot find module"):
- `npm test -- --testPathPattern=disclosure` → fails (no api/disclosure.js)
- `npm test -- --testPathPattern=disclosure-checklist` → fails (no api/helpers/disclosure-checklist.js)
- `npm test -- --testPathPattern=generate-disclosure` → fails (no api/generate-disclosure.js)
</verification>

<success_criteria>
19 total test stubs across 3 files, all RED. `npm test` runs without crash. Wave 0 complete — every subsequent implementation plan has a pre-existing automated verify command.
</success_criteria>

<output>
After completion, create `.planning/phases/02-qld-legal-scaffolding/02-01-SUMMARY.md`
</output>
