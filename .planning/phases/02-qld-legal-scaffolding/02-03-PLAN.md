---
phase: 02-qld-legal-scaffolding
plan: 03
type: execute
wave: 1
depends_on:
  - 02-01
files_modified:
  - dashboard.html
autonomous: false
requirements:
  - LEGAL-03

must_haves:
  truths:
    - "When a seller accepts an offer, a conveyancer referral prompt appears before the modal closes"
    - "The prompt names a specific QLD-licensed conveyancer (placeholder partner) with phone and website"
    - "The prompt includes a disclaimer that DEED is not a law firm and a referral arrangement may exist"
    - "Seller can dismiss the prompt and it does not reappear on subsequent page loads for the same accepted offer"
  artifacts:
    - path: dashboard.html
      provides: Conveyancer referral prompt injected into accept-modal step-3 panel
      contains: "conveyancer-referral"
  key_links:
    - from: dashboard.html (accept-modal step-3)
      to: conveyancer referral card
      via: "step-3 reveal in confirmAccept() function"
      pattern: "conveyancer-referral"
---

<objective>
Inject a conveyancer referral prompt into dashboard.html at the offer acceptance step. This is a UI-only change — no API, no integration, no database. The prompt appears after a seller accepts an offer and must include legal disclaimers per QLS Rule 12.4.4.

Purpose: LEGAL-03 — at offer acceptance, seller is shown a conveyancer referral prompt with a QLD-licensed partner.
Output: Modified dashboard.html with referral prompt in accept-modal step-3.
</objective>

<execution_context>
@/Users/edsteward/.claude/get-shit-done/workflows/execute-plan.md
@/Users/edsteward/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/edsteward/deed/.planning/PROJECT.md
@/Users/edsteward/deed/.planning/phases/02-qld-legal-scaffolding/02-RESEARCH.md

Dashboard to modify:
@/Users/edsteward/deed/dashboard.html
</context>

<interfaces>
<!-- Accept modal structure (from dashboard.html grep) -->
The accept modal has three steps:
- accept-step-1: Offer summary + optional message → "Confirm acceptance" button calls confirmAccept()
- accept-step-2: Generating contract (loading state)
- accept-step-3: Contract generated, shown to seller → currently has "Close" button

The conveyancer referral card goes inside accept-step-3, ABOVE the existing "Close" button.

<!-- QLS Rule 12.4.4 referral disclosure requirement (from RESEARCH.md) -->
If DEED has a commercial referral arrangement, it must be disclosed. Build the prompt with disclosure language included — even if no arrangement exists yet, this is the safer default. Use placeholder partner details until a real arrangement is established.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add conveyancer referral card to accept-modal step-3</name>
  <files>dashboard.html</files>
  <action>
Modify `dashboard.html` to add a conveyancer referral card inside `accept-step-3`, above the existing Close button.

**CSS (add to existing `<style>` block):**
```css
.conveyancer-card {
  background: #eff6ff;
  border: 1px solid #bfdbfe;
  border-radius: 10px;
  padding: 1.125rem 1.25rem;
  margin: 0.75rem 0 1rem;
}
.conveyancer-card-label {
  font-size: 0.65rem;
  font-weight: 600;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  color: #2563eb;
  margin-bottom: 0.4rem;
}
.conveyancer-card-headline {
  font-size: 0.95rem;
  font-weight: 600;
  color: var(--text);
  margin-bottom: 0.5rem;
}
.conveyancer-card p {
  font-size: 0.82rem;
  color: var(--muted);
  line-height: 1.6;
  margin: 0 0 0.4rem;
}
.conveyancer-card .conveyancer-disclaimer {
  font-size: 0.72rem;
  color: var(--dim);
  margin-top: 0.5rem;
  font-style: italic;
}
```

**HTML (insert inside accept-step-3 div, above the modal-footer div):**
```html
<div id="conveyancer-referral" class="conveyancer-card">
  <div class="conveyancer-card-label">Next Step: Engage a Solicitor</div>
  <div class="conveyancer-card-headline">You need a QLD-licensed solicitor to prepare the contract of sale.</div>
  <p>DEED is not a law firm and cannot prepare legal documents. A solicitor must prepare your contract of sale before it is binding.</p>
  <p>
    <strong>[Partner Firm — Placeholder]</strong><br>
    QLD-licensed conveyancing solicitors<br>
    Phone: [+61 7 XXXX XXXX] &nbsp;·&nbsp; <a href="#" style="color:#2563eb;">[firmwebsite.com.au]</a>
  </p>
  <p class="conveyancer-disclaimer">
    DEED may receive a referral arrangement with recommended firms. You are not required to use any firm we suggest — you may engage any QLD-licensed solicitor.
  </p>
</div>
```

No JavaScript changes required — the card is always present in step-3 and becomes visible when the modal advances to that step. The existing modal step logic (show/hide via `display` style) already handles this.

Place the HTML immediately before the `<div class="modal-footer">` inside `accept-step-3`.
  </action>
  <verify>
    <automated>grep -n "conveyancer-referral\|conveyancer-card\|conveyancer-disclaimer" /Users/edsteward/deed/dashboard.html</automated>
  </verify>
  <done>dashboard.html contains conveyancer-referral div, conveyancer-card CSS class, and disclaimer text. Automated grep confirms presence of all three markers.</done>
</task>

</tasks>

<verification>
- `grep -c "conveyancer-referral" /Users/edsteward/deed/dashboard.html` — returns at least 1
- `grep "QLD-licensed" /Users/edsteward/deed/dashboard.html` — returns match
- `grep "referral arrangement" /Users/edsteward/deed/dashboard.html` — returns match (QLS disclosure)
</verification>

<success_criteria>
LEGAL-03 complete: conveyancer referral card present in accept-modal step-3 with partner placeholder, legal disclaimer, and QLS Rule 12.4.4 referral disclosure language.
</success_criteria>

<output>
After completion, create `.planning/phases/02-qld-legal-scaffolding/02-03-SUMMARY.md`

Then pause for human verification:
- Open dashboard.html in browser
- Accept a test offer
- Confirm conveyancer referral card appears in the post-acceptance step before Close button
- Confirm all text is readable and referral disclaimer is present
</output>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Conveyancer referral prompt injected into the offer acceptance modal step-3 in dashboard.html</what-built>
  <how-to-verify>
    1. Open https://deed-sooty.vercel.app/dashboard.html (or local file) as a seller
    2. Find an active offer and click Accept
    3. Confirm the offer summary and message step appears (step-1)
    4. Proceed through to the contract generation step (step-2)
    5. After contract generation, confirm step-3 shows the conveyancer referral card ABOVE the Close button
    6. Verify the card has: blue background, "Next Step: Engage a Solicitor" label, solicitor placeholder text, and italic referral disclaimer
    7. Confirm the Close button is still present below the card and dismisses the modal correctly
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues with the layout or text</resume-signal>
</task>
